# Core
openai>=1.0.0
requests>=2.28.0

# Optional: Parakeet ONNX STT (local CPU inference)
# Uncomment the following lines to enable:
# onnxruntime>=1.16.0
# onnx-asr>=0.7.0
# soundfile>=0.12.0
# librosa>=0.10.0

# Optional: Edge TTS (free, no API key)
# Uncomment to enable:
# edge-tts>=6.1.0

# Optional: Kokoro ONNX TTS (local CPU)
# Uncomment to enable:
# kokoro-onnx>=0.4.0
# soundfile>=0.12.0

# Optional: NeuTTS Air (Neuphonic)
# Uncomment to enable:
# neuttsair
